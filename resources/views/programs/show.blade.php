@extends('layouts.app')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><div class="small text-muted mb-1"><a href="{{route('programs.index')}}" class="text-decoration-none">Program</a> / Detail</div><h3 class="fw-bold mb-1">{{ $program->nama }}</h3><p class="text-muted mb-0">{{ $program->kode ?: 'Tanpa kode' }} · Struktur Program, Kegiatan, dan Sub Kegiatan</p></div><a href="{{route('programs.index')}}" class="btn btn-outline-secondary">← Kembali</a></div>
<div class="row g-3 mb-4"><div class="col-md-4"><div class="card border-0 shadow-sm h-100"><div class="card-body"><div class="small text-muted">Program</div><div class="fw-bold fs-5">{{ $program->nama }}</div><div class="small text-muted mt-2">Target: {{ $program->target ?? '-' }} {{ $program->satuan ?? '' }}</div></div></div></div><div class="col-md-4"><div class="card border-0 shadow-sm h-100"><div class="card-body"><div class="small text-muted">Jumlah Kegiatan</div><div class="fw-bold fs-3">{{ $program->kegiatans->count() }}</div></div></div></div><div class="col-md-4"><div class="card border-0 shadow-sm h-100"><div class="card-body"><div class="small text-muted">Jumlah Sub Kegiatan</div><div class="fw-bold fs-3">{{ $program->kegiatans->sum(fn($k) => $k->subKegiatans->count()) }}</div></div></div></div></div>
<div class="card border-0 shadow-sm"><div class="card-header bg-white py-3"><div class="fw-bold">Kegiatan & Sub Kegiatan</div><div class="small text-muted">Klik Kegiatan untuk melihat Sub Kegiatan di dalamnya.</div></div><div class="card-body p-0"><div class="accordion accordion-flush" id="kegiatanAccordion">@forelse($program->kegiatans as $i=>$k)<div class="accordion-item"><h2 class="accordion-header" id="heading{{$k->id}}"><button class="accordion-button {{ $i ? 'collapsed' : '' }}" type="button" data-bs-toggle="collapse" data-bs-target="#collapse{{$k->id}}"><span class="badge text-bg-primary me-3">{{ $k->kode ?: 'Kegiatan' }}</span><span class="fw-semibold">{{ $k->nama }}</span><span class="badge rounded-pill text-bg-light ms-auto me-2">{{ $k->subKegiatans->count() }} Sub Kegiatan</span></button></h2><div id="collapse{{$k->id}}" class="accordion-collapse collapse {{ !$i ? 'show' : '' }}" data-bs-parent="#kegiatanAccordion"><div class="accordion-body bg-light"><div class="d-flex justify-content-between align-items-center mb-2"><strong>Sub Kegiatan</strong><a class="btn btn-sm btn-outline-primary" href="{{route('sub-kegiatans.index',['kegiatan_id'=>$k->id])}}">Kelola Sub Kegiatan</a></div>@if($k->subKegiatans->count())<div class="list-group">@foreach($k->subKegiatans as $sub)<div class="list-group-item d-flex justify-content-between align-items-center"><div><div class="fw-semibold">{{ $sub->nama }}</div><div class="small text-muted">{{ $sub->kode ?: 'Tanpa kode' }} · Target: {{ $sub->target ?? '-' }} {{ $sub->satuan ?? '' }}</div></div><span class="badge text-bg-secondary">Sub Kegiatan</span></div>@endforeach</div>@else<div class="text-muted small py-2">Belum ada Sub Kegiatan pada Kegiatan ini.</div>@endif</div></div></div>@empty<div class="p-5 text-center text-muted">Belum ada Kegiatan pada Program ini.</div>@endforelse</div></div></div>
@endsection