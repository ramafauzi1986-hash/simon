@extends('layouts.app')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h3 class="fw-bold mb-1">Program</h3><p class="text-muted mb-0">Pilih Program untuk membuka Kegiatan dan Sub Kegiatan di dalamnya.</p></div><button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#addProgram">+ Tambah Program</button></div>
@if(session('success'))<div class="alert alert-success">{{session('success')}}</div>@endif
<div class="row g-3">@forelse($programs as $p)<div class="col-xl-6"><div class="card h-100 shadow-sm border-0"><div class="card-body"><div class="d-flex justify-content-between gap-3"><div><div class="small text-muted">{{ $p->kode ?: 'Tanpa kode' }}</div><h5 class="fw-bold mb-2">{{ $p->nama }}</h5><div class="text-muted small">Target: {{ $p->target ?? '-' }} {{ $p->satuan ?? '' }}</div></div><span class="badge rounded-pill text-bg-primary align-self-start">{{ $p->kegiatans_count }} Kegiatan</span></div><hr><div class="d-flex justify-content-between align-items-center"><span class="small text-muted">Hierarki: Program → Kegiatan → Sub Kegiatan</span><a class="btn btn-sm btn-primary" href="{{route('programs.show',$p)}}">Buka Program →</a></div></div></div></div>@empty<div class="col-12"><div class="card border-0 shadow-sm"><div class="card-body text-center text-muted py-5">Belum ada data Program.</div></div></div>@endforelse</div>
<div class="mt-4">{{$programs->links()}}</div>
<div class="modal fade" id="addProgram"><div class="modal-dialog"><form class="modal-content" method="POST" action="{{route('programs.store')}}">@csrf<div class="modal-header"><h5>Tambah Program</h5><button type="button" class="btn-close" data-bs-dismiss="modal"></button></div><div class="modal-body"><div class="mb-3"><label class="form-label">Kode</label><input name="kode" class="form-control"></div><div class="mb-3"><label class="form-label">Nama Program</label><input name="nama" class="form-control" required></div><div class="row"><div class="col"><label class="form-label">Target</label><input name="target" type="number" step="0.01" class="form-control"></div><div class="col"><label class="form-label">Satuan</label><input name="satuan" class="form-control"></div></div></div><div class="modal-footer"><button class="btn btn-primary">Simpan</button></div></form></div></div>
@endsection