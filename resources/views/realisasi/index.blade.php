@extends('layouts.app')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h3 class="fw-bold">Realisasi Kinerja & Evidence</h3><p class="text-muted mb-0">Input capaian Triwulan I–IV dan dokumen pendukung.</p></div><button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#addReal">+ Input Realisasi</button></div>
@if(session('success'))<div class="alert alert-success">{{session('success')}}</div>@endif @if($errors->any())<div class="alert alert-danger">{{$errors->first()}}</div>@endif
<div class="card shadow-sm border-0"><div class="card-body"><div class="table-responsive"><table class="table table-hover align-middle"><thead><tr><th>Indikator</th><th>TW</th><th>Nilai</th><th>Evidence</th><th></th></tr></thead><tbody>@forelse($items as $x)<tr><td class="fw-semibold">{{$x->indikator?->nama}}</td><td>TW {{$x->triwulan}}</td><td>{{$x->nilai}}</td><td>@if($x->evidence_path)<a class="btn btn-sm btn-outline-success" href="{{route('realisasi.download',$x)}}">Lihat/Unduh</a>@else<span class="badge bg-secondary">Belum ada</span>@endif</td><td class="text-end"><form method="POST" action="{{route('realisasi.destroy',$x)}}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" onclick="return confirm('Hapus realisasi?')">Hapus</button></form></td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-4">Belum ada realisasi.</td></tr>@endforelse</tbody></table></div>{{$items->links()}}</div></div>
<div class="modal fade" id="addReal"><div class="modal-dialog modal-lg"><form class="modal-content" method="POST" action="{{route('realisasi.store')}}" enctype="multipart/form-data">@csrf<div class="modal-header"><h5>Input Realisasi & Evidence</h5><button type="button" class="btn-close" data-bs-dismiss="modal"></button></div><div class="modal-body"><div class="mb-3"><label class="form-label">Indikator Kinerja</label><select name="indikator_kinerja_id" class="form-select" required><option value="">Pilih Indikator</option>@foreach($indikators as $i)<option value="{{$i->id}}">{{$i->nama}}</option>@endforeach</select></div><div class="row"><div class="col-md-4"><label class="form-label">Triwulan</label><select name="triwulan" class="form-select"><option value="1">TW I</option><option value="2">TW II</option><option value="3">TW III</option><option value="4">TW IV</option></select></div><div class="col-md-8"><label class="form-label">Nilai Realisasi</label><input name="nilai" type="number" step="0.01" min="0" class="form-control" required></div></div><div class="mt-3"><label class="form-label">Keterangan</label><textarea name="keterangan" class="form-control"></textarea></div><div class="mt-3"><label class="form-label">Evidence</label><input name="evidence" type="file" class="form-control" accept=".pdf,.jpg,.jpeg,.png,.doc,.docx,.xls,.xlsx"><small class="text-muted">Maksimal 10 MB. PDF, Word, Excel atau gambar.</small></div></div><div class="modal-footer"><button class="btn btn-primary">Simpan Realisasi</button></div></form></div></div>
@endsection